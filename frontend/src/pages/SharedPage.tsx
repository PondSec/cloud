export function SharedPage() {
  return (
    <div className="h-full p-4">
      <div className="rounded-2xl border border-white/10 bg-black/20 p-8 text-sm text-zinc-300">
        Shared links are scaffolded in the backend (`ShareLink` model + permission hooks). UI sharing flows can be added next.
      </div>
    </div>
  );
}
